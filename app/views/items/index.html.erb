<div class = "item-main">

  <!-- 運営よう新規登録ボタン-->
  <div class = "item-new">
    <% if current_user.id == 1 %>
      <%= link_to "グッズを新規登録する", new_item_path, method: :get %>
    <% end %>
  </div>
   <!-- -->

  <div>運営応援グッズ購入ページ</div>
    <div class = "item-font">買ってくれると運営のご飯のおかずが一品増えます。</div>
   
    <% @items.each do |item| %>
      <div class = "item-box">
        <!-- グッズ表示 -->
        <div class = "item-name">
            <%= item.name %>
        </div>   
        <div class = "item-img">
          <%= image_tag item.item_image, class:"item-img"%>
        </div>

        <div class = "item-introduction">
          <%= simple_format item.introduction %>
        </div>

        <div class = "item-price">
          <%= item.price %>円<p class = "mini-font">（税込）</p>
        </div>
        
        <% if user_signed_in? %>
          <%= link_to '購入画面に進む', item_orders_path(item.id),class:"item-btn"%>
        <% end %>
          
        <div class = "item-font">
          <% if current_user.id == 1 %>
            この商品を
            <%= link_to "編集する", edit_item_path(item.id), class: "", method: :get%>
            <%= link_to "削除する", item_path(item.id), method: :delete, class: "" %>
          <% end %>
          </div>
      </div>
   <% end %>
  <!-- -->
</div>